version: "2"

linters:
  exclusions:
    rules:
      # goconst flags repeated string literals; tests naturally repeat
      # strings (channel names, IDs, status values) and should not be
      # forced to hoist them to constants.
      - path: _test\.go
        linters:
          - goconst
